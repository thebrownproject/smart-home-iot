# Desktop Python tools for ESP32 development & deployment
# Install with: pip install -r requirements.txt

mpremote==1.26.1      # MicroPython remote control & file upload to ESP32
pyserial==3.5         # Serial communication with ESP32
platformdirs==4.4.0   # Cross-platform path handling (dependency of mpremote)

# Note: MicroPython libraries for ESP32 are in Docs/esp32lib/ and installed via Thonny IDE
# - umqtt (MQTT client)
# - dht (Temperature/humidity sensor)
# - neopixel (RGB LED control)
# - ssd1306 (OLED display)
# - mfrc522 (RFID reader)
